{
    "name": "web-components",
    "version": "1.0.0",
    "main": "index.js",
    "license": "MIT",
    "scripts": {
        "dev": "parcel index.html",
        "build": "parcel build index.html --public-url ./",
        "deploy": "parcel build index.html --public-url ./ && git subtree push --force --prefix dist origin gh-pages"
    },
    "devDependencies": {
        "parcel-bundler": "^1.12.4"
    }
}
